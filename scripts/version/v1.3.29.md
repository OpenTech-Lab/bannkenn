# Changelog — v1.3.29

**Release date:** 2026-03-11

## What's Changed

- 85bb986 feat: add guidance for offline Docker Rust builds and sync vendored crates
- 728e62a feat: add native TLS support for BannKenn server API with optional local HTTP fallback - Implemented TLS configuration in ServerConfig with new fields for cert and key paths - Updated server to handle both HTTPS and plain HTTP based on configuration - Modified Docker Compose for TLS support and local dashboard binding - Enhanced README with guidance on native TLS setup and usage - Adjusted agent prompt text for clarity on API server URL

## Install

**Linux x64:**
```bash
curl -Lo bannkenn-agent https://github.com/OpenTech-Lab/bannkenn/releases/download/v1.3.29/bannkenn-agent-linux-x64
chmod +x bannkenn-agent && sudo mv bannkenn-agent /usr/local/bin/
```

**Linux ARM64:**
```bash
curl -Lo bannkenn-agent https://github.com/OpenTech-Lab/bannkenn/releases/download/v1.3.29/bannkenn-agent-linux-arm64
chmod +x bannkenn-agent && sudo mv bannkenn-agent /usr/local/bin/
```

**Windows (PowerShell):**
```powershell
Invoke-WebRequest -Uri https://github.com/OpenTech-Lab/bannkenn/releases/download/v1.3.29/bannkenn-agent-windows-x64.exe -OutFile bannkenn-agent.exe
```

## Full Changelog

https://github.com/OpenTech-Lab/bannkenn/compare/v1.3.28...v1.3.29
